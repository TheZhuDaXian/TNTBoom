package name.tntboom;

import net.fabricmc.api.ClientModInitializer;

public class TNTBoomClient implements ClientModInitializer {
    @Override
    public void onInitializeClient() {
        // Client-side initialization code (if needed)
    }
}